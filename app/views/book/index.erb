<div class="container">
  <h1><%= @account.name -%>が登録した本</h1>
</div>

<div class="container">
  <% @books.each do |book| %>
  <%= partial 'layouts/book', :locals=>{:book=>book} %>
  <% end %>
</div>
